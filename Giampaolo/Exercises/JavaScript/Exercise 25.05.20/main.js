let max = 10;
let min = 1;

function changeColor() {
    let x = Math.floor(Math.random() * (max - min + 1) + min);

    /**
    * Write a code that change the color according to the result of the X;
    * You can use aN if-else statement or a switch.

    * Hint:
    * For changing the color you have to use the command '.style.backgroundColor'
    */
}